machine:
  php:
    version: 5.5.21
  hosts:
    localhost: 127.0.0.1
checkout:
  post:
    - php artisan vendor:publish --force
    - php artisan build
test:
  pre:
    - php artisan serve --port=80
