machine:
  php:
    version: 5.5.21
checkout:
  post:
    - cp .env.circle .env
dependencies:
  override:
    - source bin/install_backend.sh
database:
  override:
test:
  pre:
    - php artisan serve --port=80:
        background: true
