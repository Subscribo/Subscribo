machine:
  php:
    version: 5.5.21
checkout:
  post:
    - php artisan vendor:publish --force
    - php artisan build
database:
  override:
    - php artisan migrate:refresh --seed
test:
  pre:
    - php artisan serve --port=80:
      background: true
